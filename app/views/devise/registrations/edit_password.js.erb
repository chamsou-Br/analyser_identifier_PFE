var modal;

modal = $('#modal .md-content div').html("<%=j render :partial => 'form_edit_password', :content_type => 'text/html' %>");

// display errors message if any
<% if @user.errors.any? %>
  $('#modal div.error_messages').fadeIn(1200)
  $("#modal").effect("shake", function(){ 
  }); 
<% end %>
