var modal;

modal = $('#modal .md-content div').html("<%=j render :partial => 'form_new', :content_type => 'text/html' %>");

<% if resource.errors.any? %>
  $('#modal div.error_messages').fadeIn(1200)
  $('#modal').effect("shake", function(){
  });
<% elsif check_if_freemium(current_user) %>
	$('#modal div.error_messages').fadeIn(500)
<% end %>



modal.find(':text:first').focus();
